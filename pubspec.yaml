name: grpc_dart_server
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  grpc:
  protobuf:

dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.16.0
